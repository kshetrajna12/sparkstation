# constraints.txt - DGX Spark tested versions
# Version pinning to ensure stability across vLLM, SGLang, and LiteLLM updates

# LLM Backends (system-level installs, not managed by uv)
# vllm==0.4.2
# sglang==0.2.5

# Gateway
litellm==1.35.8

# Core dependencies
fastapi==0.110.0
uvicorn==0.29.0
httpx==0.27.0
pydantic==2.6.4
pydantic-settings==2.2.0
prometheus-client==0.20.0

# NVIDIA/CUDA
nvidia-ml-py3==7.352.0

# Database
sqlalchemy==2.0.29
aiosqlite==0.20.0

# Utilities
pyyaml==6.0.1
python-dotenv==1.0.1
click==8.1.7

# Update Strategy:
# 1. Test new versions in dev environment first
# 2. Update one backend at a time
# 3. Verify all flags still work
# 4. Update this file
# 5. Deploy to production
# Quarterly review for security updates
